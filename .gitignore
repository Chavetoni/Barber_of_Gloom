# Internal dev docs — not part of the published site
CLAUDE.md
*NOTES.md

# macOS
.DS_Store

# design-canvas runtime sidecar/state files
.image-slots.state.json
.design-canvas.state.json
*.state.json

# local backup of archived originals (not part of the site)
_archive/

# dev-only helpers (mobile preview, nav experiments) — not part of the site
_dev/

CLAUDE.md
.claude

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
